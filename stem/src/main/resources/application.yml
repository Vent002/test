server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
  servlet:
    context-path: /stem
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/final_exam?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 123456

  http:
    encoding:
      charset: UTF-8
      force: true
      enabled: true
mybatis:
  mapper-locations: mappers/*.xml
